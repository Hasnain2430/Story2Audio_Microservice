/**
 * Phase 0 placeholder.
 *
 * The real application is built in Phase 6 against the gateway's OpenAPI schema. This
 * file exists so that `npm run build` and `npm run lint` have something to check, and so
 * the toolchain is proven before any feature code depends on it.
 */
export function App() {
  return (
    <main>
      <h1>Story2Audio</h1>
      <p>v2 rebuild — frontend lands in Phase 6.</p>
    </main>
  )
}
